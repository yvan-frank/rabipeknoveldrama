{
  "name": "rabipek",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names API,WEB --prefix-colors blue,magenta \"npm --prefix ./refonte_server run dev\" \"npm --prefix ./refonte_rabi_frontend run dev\"",
    "dev:api": "npm --prefix ./refonte_server run dev",
    "dev:web": "npm --prefix ./refonte_rabi_frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
