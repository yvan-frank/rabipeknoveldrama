# URL de base de l'API rabipek_server (refonte). Utilisé côté serveur (Server
# Components, Route Handlers) ET côté client — d'où le préfixe NEXT_PUBLIC_.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
